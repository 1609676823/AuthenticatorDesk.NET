# 为 AuthenticatorDesk 贡献

[English](CONTRIBUTING.md)

感谢你帮助改进 AuthenticatorDesk。我们欢迎代码、测试、文档、翻译、无障碍与用户体验方面的贡献。

## 开始之前

- 新建议或问题提交前，请先搜索已有 Issue 和 Pull Request。
- 复现问题时，请使用默认分支的最新版本。
- 不要公开认证密钥、当前验证码、二维码、OTP Auth URI、`vault.json`、
  `.authdesk` 备份、WinAuth 导出文件或包含隐私的日志内容。
- 疑似安全漏洞请按 [SECURITY.zh-CN.md](SECURITY.zh-CN.md) 私下报告。
- 每次改动应保持聚焦。大规模界面重构、存储格式变更、密码学变更或引入新依赖前，
  请先讨论设计。

## 开发环境

AuthenticatorDesk 是面向 .NET 10 的 Windows Forms 应用。完整构建、运行应用和测试套件
需要 Windows 与 .NET 10 SDK。

```powershell
dotnet restore .\AuthenticatorDesk.NET.slnx
dotnet build .\AuthenticatorDesk.NET.slnx -c Release --no-restore
dotnet test .\AuthenticatorDesk.NET.slnx -c Release --no-build
dotnet run --project .\AuthenticatorDesk.NET\AuthenticatorDesk.NET.csproj -c Release
```

应用通常将 `vault.json` 保存在可执行文件旁。除非选择其他数据目录，开发运行会在构建输出
目录中创建本地数据。切勿提交这些数据。

## 提交改动

1. Fork 仓库并创建简短、含义明确的分支。
2. 用最小且完整的改动解决问题。
3. 行为发生变化时增加或更新测试。
4. 运行 Release 构建和完整测试套件。
5. 行为、兼容性、存储或安全预期变化时更新面向用户的文档。
6. 发起 Pull Request，说明问题、解决方案、验证方式和仍然存在的限制。

## 代码规范

- 遵循现有 C# 风格并保持可空引用类型启用。
- 保持用户密钥位于本地。未经设计和隐私审查，不要添加遥测、联网、崩溃上报或更新检查。
- 在认证密钥、恢复数据、二维码载荷和 OTP Auth URI 的整个生命周期中将其视为敏感数据。
- 修改保险库或备份代码时，保留原子写入、恢复文件与密钥清零行为。
- 除非有文档说明并补充测试，不要降低导入文件或语言包的验证限制。
- 面向用户的文本应放入语言包资源；有意保持不翻译的产品名、服务商名、协议名和算法名除外。
- 保持 Windows Forms 布局在不同 DPI、窗口宽度和文本长度下可用。
- 不要在同一 Pull Request 中夹带无关的格式化或机械修改。

## 测试

xUnit 冒烟测试覆盖 OTP 标准、二维码和迁移格式、保险库与备份切换、存储恢复、本地化及
响应式界面行为。测试使用 Windows Forms 与单线程单元状态，因此必须在 Windows 上运行。

修复缺陷时应尽可能添加回归测试。安全、密码学、导入、存储和迁移变更需要同时覆盖成功与
失败路径。测试中不要使用真实认证密钥；请使用公开 RFC 向量或明确标记的合成测试数据。

## 翻译

英语是源语言基线。请参阅[语言包说明](AuthenticatorDesk.NET/Languages/README.zh-CN.md)
及其[英文版本](AuthenticatorDesk.NET/Languages/README.md)。

内置语言包必须包含完整的英文键集合，并保留全部复合格式占位符。社区语言包可以只包含
部分键，因为缺失值会回退到内嵌英语。

新增或修改面向用户的英文字符串时：

1. 更新 `Languages/en.json`；
2. 更新所有内置语言包；
3. 验证占位符和元数据；
4. 运行完整测试套件；
5. 使用较长译文检查受影响的界面布局。

机器翻译可以作为起点，但贡献者应复核语义、术语、格式和界面适配。

## 文档与截图

- 仓库级文档默认使用英语；存在对应 `*.zh-CN.md` 时，应保持两者同步。
- 默认 README 截图使用英文界面。
- 截图中只能使用合成账户和公开测试密钥。
- 提交图片前移除鼠标光标、个人路径、用户名、通知内容及其他可识别信息。
- 使用相对链接，确保文档可同时在 GitHub 与 Gitee 使用。

## 依赖与第三方材料

新依赖会增加安全、维护和许可证义务。请说明引入理由，并且只使用能够随
GPL-3.0-or-later 组合作品分发的软件包。“开源”本身并不足够；还必须检查许可证
兼容性、署名和源代码义务。必要时同步更新第三方声明和许可证文件。

除非你有权将材料许可给本项目，否则不要贡献相应的代码、图标、翻译、截图或其他内容。
对于改编代码，应标明确切的上游版本，保留版权和许可证声明，并说明修改内容和日期。

## Pull Request 检查清单

- [ ] 改动保持聚焦，并已说明对用户的影响。
- [ ] Windows 上的 Release 构建和测试通过。
- [ ] 新行为已有测试，并覆盖相关失败场景。
- [ ] 未包含真实密钥、保险库、备份、二维码、OTP URI 或隐私日志。
- [ ] 面向用户的字符串已完成本地化。
- [ ] 适用时英文与中文文档保持同步。
- [ ] 已说明安全、隐私、兼容性和迁移影响。
- [ ] 已更新所需的第三方声明和许可证文件。

## 贡献许可

提交贡献即表示你确认有权提交该内容，并同意它可以按本项目的
[GNU GPL 第 3 版或任何更高版本](LICENSE.txt)分发。第三方材料仍受其自身兼容
许可证和声明要求约束。
