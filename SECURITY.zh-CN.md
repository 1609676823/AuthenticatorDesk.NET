# 安全策略

[English](SECURITY.md)

AuthenticatorDesk 保存的凭据可能用于访问其他服务。请据此谨慎处理安全报告和测试数据。

## 支持的版本

在长期发布策略确定前，安全修复面向最新公开版本和当前默认分支。旧版二进制文件和非官方
构建可能不会获得修复。条件允许时，请使用最新代码复现问题。

## 报告漏洞

不要在公开 Issue、讨论、Pull Request、截图或 Gitee 反馈中披露疑似漏洞。

优先使用主仓库提供的私有漏洞报告功能。如果页面没有私有渠道，请只创建标题为
**Security contact request** 的公开 Issue，不要附带任何技术细节；维护者会再安排私下沟通。

请根据情况提供：

- 受影响的版本、提交和 Windows 版本；
- 涉及的保护模式：便携、主密码或 Windows 账户；
- 对影响和受影响组件的简要说明；
- 使用合成数据的复现步骤或最小概念验证；
- 是否需要用户交互或保险库已处于解锁状态；
- 建议的缓解措施或修复方案；
- 是否希望在公开信息中署名。

切勿发送真实共享密钥、当前验证码、恢复码、个人二维码、OTP Auth URI、`vault.json`、
`.authdesk` 备份、WinAuth 配置或未经检查的诊断日志。请创建能够演示问题的合成测试数据。
如果文件不可或缺，请移除无关记录，并且只通过双方确认的私有渠道传输。

项目目标是在七天内确认收到完整报告，并在十四天内给出初步评估。志愿维护者的时间和问题
复杂度可能影响这些目标。

## 安全模型

AuthenticatorDesk 采用本地优先设计。当前应用没有账户服务、云同步、遥测、自动崩溃上报
或网络客户端。验证码在本机生成。

保险库支持三种保护模式：

| 模式 | 密钥保护 | 可移植性与限制 |
| --- | --- | --- |
| 便携 | AES 密钥保存在同一个 `vault.json` 封装中 | 便于复制，但**不提供**账户级访问保护。任何取得该文件的人都可以打开它。新保险库默认使用此模式。 |
| 主密码 | 使用随机盐和 600,000 次 PBKDF2-HMAC-SHA256 迭代派生 AES 密钥 | 可以用同一密码在其他电脑打开文件。密码无法找回。 |
| Windows 账户 | 随机 AES 密钥由当前用户的 Windows DPAPI 保护 | 本机使用方便，但其他 Windows 账户或电脑通常无法打开。迁移前应关闭此模式。 |

保险库和加密备份载荷使用 AES-256-GCM 进行认证加密。受密码保护的 `.authdesk` 备份使用
独立的密码和密钥派生。该实现尚未经过正式的独立安全审计；使用密码学术语并不保证每一种
主机环境或操作流程都绝对安全。

完整实现边界和威胁模型参见
[docs/SECURITY-MODEL.zh-CN.md](docs/SECURITY-MODEL.zh-CN.md)。

## 重要限制

- 便携模式是互操作功能，不是访问控制。
- 控制当前 Windows 账户、管理员上下文或运行中进程的攻击者可能访问已解锁的保险库。
- 应用无法防止截屏、恶意输入软件、剪贴板监控或已被攻陷的操作系统。
- 定时剪贴板清理只覆盖从仪表盘或全局复制快捷键复制的验证码；它属于尽力而为，并且仅在
  剪贴板仍包含同一验证码时执行，不覆盖含密钥的对话框。
- 全局快捷键自动输入会把验证码发送到当前焦点窗口，用户必须确认目标窗口。
- 二维码、OTP Auth URI、明文 WinAuth 导出和 OTP URI 文本导出都包含认证密钥，不能公开
  或长期保存在不可信位置。
- Windows 账户保护依赖 Windows DPAPI 和原用户配置文件持续可用。
- 错误日志可能包含异常详情和本地路径，分享前应检查并脱敏。
- 第三方库及 .NET/Windows 运行时仍属于可信计算基础。

## 范围内示例

- 保险库、备份、密码、密钥处理或认证绕过；
- 密钥或生成验证码的泄露和意外持久化；
- 不安全的导入解析、路径处理、文件替换或迁移；
- 二维码、OTP URI、WinAuth、Base32 或语言包解析器漏洞；
- 剪贴板、全局快捷键、自动输入、托盘或锁定状态边界失效；
- 可在 AuthenticatorDesk 中触发的依赖漏洞。

没有安全影响的易用性问题、不受支持环境中的失败，以及必须先攻陷操作系统才能成立的报告，
可能按普通 Issue 处理。

## 协调披露

公开披露前，请为维护者留出合理时间复现、修复、测试并发布更新。若报告者希望，发布说明
或安全公告可以致谢署名。本项目目前没有付费漏洞奖励计划。
